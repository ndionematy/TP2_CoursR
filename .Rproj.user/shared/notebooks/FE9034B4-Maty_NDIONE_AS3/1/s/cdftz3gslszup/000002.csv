"0","```r
# Classification kmeans
km_cluster <- kmeans(base, centers = 5, nstart=20)

base$cluster <- km_cluster$cluster

ggplot(base, aes(minutes, points, col = factor(cluster))) + 
geom_point(size = 1, alpha = 0.8, position = \"jitter\")

```"
